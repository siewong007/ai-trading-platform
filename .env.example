# Copy to .env and fill in. Required by `trade`/`flatten` (never logged, never stored in DB).
# BINANCE_API_KEY=
# BINANCE_API_SECRET=
# Optional Telegram alerts on start/halt/flatten/errors (both must be set).
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
